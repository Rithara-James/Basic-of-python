# Basic-of-python
This basic Python exercises: checking prime numbers, verifying the product of random numbers, printing squares of even or odd numbers, counting word frequency in text, and a function to check if a string is a palindrome.
